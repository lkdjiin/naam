#!/usr/bin/env ruby

$LOAD_PATH.unshift File.dirname(__FILE__) + '/../lib'
$NAAM_PATH = File.expand_path(File.dirname(__FILE__)) + '/..'

require 'naam'

if ARGV[0].nil?
  puts "usage: naam filename"
else
  main = Naam::Main.run ARGV[0].dup
end

